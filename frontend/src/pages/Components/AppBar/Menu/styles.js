import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  menuButton: {
    marginRight: 36,
  },
  hide: {
    display: "none",
  },
}));

export default useStyles;
